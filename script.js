// creating a form for the date and image to be displayed

const form = document.getElementById('date-form');
const imageinfo = document.getElementById('image-container');


// using the event listener - its job is to to the event being submitted
form.addEventListener('submit', function(event) {
//preventDefault - stops people from hitting submit and it just refreshing the page
    event.preventDefault();
    const dateInput = form.elements




}